// const arr = ["a", "b", "c"];

// {arr.map((e, i) => {
//             return <div key={e}>{e} {i}</div>
//         })}

export default function ButtonGame() {
  return (
    <div className="">

    </div>
  );
}